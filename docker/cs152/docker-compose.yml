version: '3.9'
services:
  cs183-main:
    image: local/cs152
    build: .
    privileged: true
    hostname: cs152-machine
    ports:
      #- '2020:22'
    volumes:
      #- type: bind
      #  source: ${HOME}
       # target: /home/