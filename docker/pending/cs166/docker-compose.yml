version: '3.9'
services:
  cs166-main:
    image: local/cs166
    build: .
    privileged: true
    hostname: cs166-machine
    #ports:
      #- '2020:22'
    #volumes:
      #- type: bind
      #  source: ${HOME}
       # target: /home/