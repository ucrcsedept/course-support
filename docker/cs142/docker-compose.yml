version: '3.9'
services:
  cs183-main:
    image: local/cs142
    build: .
    privileged: true
    hostname: cs142-machine
    ports:
      #- '2020:22'
    volumes:
      #- type: bind
      #  source: ${HOME}
       # target: /home/