version: '3.9'
services:
  cs183-main:
    image: local/cs120b
    build: .
    privileged: true
    hostname: cs120b-machine
    ports:
      #- '2020:22'
    volumes:
      #- type: bind
      #  source: ${HOME}
       # target: /home/